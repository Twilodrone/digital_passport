version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: passport_db
    restart: always
    environment:
      POSTGRES_USER: passport_user
      POSTGRES_PASSWORD: strong_password
      POSTGRES_DB: passport_db
    ports:
      - "5432:5432"
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
